//Import REact and ReactDOM libraries

import React from 'react';
import ReactDOM from 'react-dom';

//Create a React Component

const App = () => {
	return <div>Hi there chule!</div>;
};

ReactDOM.render(<App />, document.querySelector('#root'));